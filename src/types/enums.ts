export enum ESessionStorageKey {
    Token = 'token',
}

export enum ENoticeType {
    Default = '',
    Error = 'error',
    Success = 'success',
    Warning = 'warning',
}

export enum ECardsColumnType {
    Default = '',
    Plants = 'plants',
    Modules = 'modules',
    Inverters = 'inverters',
}

export enum ECardType {
    Disabled = 'disabled',
    Inactive = 'inactive',
    Active = 'active',
}
